'use strict';

var React = require('react-native');

var {
  StyleSheet,
} = React;

module.exports = StyleSheet.create({

  container: {
    marginTop: 0,
  },
  inputText: {
    //height: 40,
    //borderColor: 'gray',
    borderWidth: 1,
    borderRadius: 5,
    backgroundColor: '#f1f1f1',
    color: '#ff7200',
    borderColor: '#dc6504',
  },
  button: {
    color: 'red',
  },

});
